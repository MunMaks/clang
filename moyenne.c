#include <stdio.h>

int main(){

    //printf("Maks MUNAIT");
    
    /*
    int num1, num2;
    printf("Give me first number: \n");
    scanf("%d", &num1);

    printf("Give me second number: \n");
    scanf("%d", &num2);

    int somme = num1 + num2;
    printf("La somme: %d \n", somme);

    if (somme % 2 == 0 && somme %3 == 0) printf("%d est divisible par 2 et 3\n", somme);
    
    else if (somme %3 == 0) printf("%d est divisible par 3\n", somme);

    else if (somme %2 == 0) printf("%d est divisible par 2\n", somme);
    
    else printf("%d N'est pas divisible ni par 2 ni par 3!\n", somme);
    */

    /*int J;
    printf("Entrez le nombre de jours: \n");
    scanf("%d", &J);

    int siecle, annee, mois, semaine, jours;
    siecle = J / 36000;

    semaine = J / 7;
    mois = J / 30;
    annee = J / 360;
    
    jours = J;

    printf("%d siecles %d annees %d mois %d semaines %d jours \n", siecle, annee, mois, semaine, jours);
    */

    return 0;
}